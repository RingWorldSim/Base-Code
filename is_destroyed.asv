function res = is_destroyed(positions, definition)
%The positions matrix is the same one as usual: it defines all of the
%points in the ring. definition should be a scalar value, that defines the
%allowed deviation from the original radius

    radii = []; %a vector containing all of the distances from the sun
    for i = 1:length(positions)
        
    end

end